using System;

namespace project_iv
{
    class Program
    {
        static void Main(string[] args)
        {
            int friends = 10;
            // friends = friends + 2;
            // friends += 2;//adds 2 to the original number
            //friends++;

            //friends = friends - 1; //subtracts 1 from og value
            //friends -= 1;
            // friends--;


            //friends = friends * 2;//multiplies og value by 2
            //  friends *= 2;

            //friends = friends / 2; //divides og value by 2 , since value is integer the reaminder won't be displayed
            //friends /= 2;

            int remainder = friends % 3; // shows only the remainder after dividing










            Console.WriteLine(friends);
            Console.WriteLine(remainder);
























            Console.ReadKey();
        }
    }
}
